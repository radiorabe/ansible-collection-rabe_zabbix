name: Release

on:
  release:
    types:
      - published

jobs:
  call-workflow:
    uses: radiorabe/actions/.github/workflows/release-ansible-collection.yaml@5dafb461cea1f8b296d89297dc6de7bd0891f0d2 # v0.44.2
    secrets:
      GALAXY_API_KEY: ${{ secrets.GALAXY_API_KEY }}
