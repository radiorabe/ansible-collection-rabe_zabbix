name: Release

on:
  release:
    types:
      - published

jobs:
  call-workflow:
    uses: radiorabe/actions/.github/workflows/release-ansible-collection.yaml@3f1ee42ffa54794a512d3989c34e3b4514de64cc # v0.43.1
    secrets:
      GALAXY_API_KEY: ${{ secrets.GALAXY_API_KEY }}
