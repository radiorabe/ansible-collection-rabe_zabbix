name: Release

on:
  release:
    types:
      - published

jobs:
  call-workflow:
    uses: radiorabe/actions/.github/workflows/release-ansible-collection.yaml@f49d301c653f9aa0d99bec7b7f18f2d789e93fa7 # v0.43.4
    secrets:
      GALAXY_API_KEY: ${{ secrets.GALAXY_API_KEY }}
