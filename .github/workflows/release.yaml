name: Release

on:
  release:
    types:
      - published

jobs:
  call-workflow:
    uses: radiorabe/actions/.github/workflows/release-ansible-collection.yaml@c26933dd78f0f76b51d1e3bb80ac099bfe0ee89b # v0.45.0
    secrets:
      GALAXY_API_KEY: ${{ secrets.GALAXY_API_KEY }}
