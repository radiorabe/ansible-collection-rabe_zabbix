name: Release

on:
  release:
    types:
      - published

jobs:
  call-workflow:
    uses: radiorabe/actions/.github/workflows/release-ansible-collection.yaml@953c51da7e4c2c681f15c70513cb803c8fa461cc # v0.44.3
    secrets:
      GALAXY_API_KEY: ${{ secrets.GALAXY_API_KEY }}
